apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudcommander-worker
  namespace: cloudcommander-staging
  labels:
    app: cloudcommander
    component: worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cloudcommander
      component: worker
  template:
    metadata:
      labels:
        app: cloudcommander
        component: worker
    spec:
      initContainers:
        - name: run-migrations
          image: jakeyy8/cloudcommander-worker:34174f2
          imagePullPolicy: IfNotPresent
          command: ["python", "-m", "migrations.run"]
          envFrom:
            - secretRef:
                name: cloudcommander-secrets
            - configMapRef:
                name: cloudcommander-config
      containers:
        - name: worker
          image: jakeyy8/cloudcommander-worker:34174f2
          imagePullPolicy: IfNotPresent
          command: ["python", "-m", "app.worker.projection_worker"]
          envFrom:
            - secretRef:
                name: cloudcommander-secrets
            - configMapRef:
                name: cloudcommander-config
          env:
            - name: MTLS_CERT_PATH
              value: /etc/mtls/tls.crt
            - name: MTLS_KEY_PATH
              value: /etc/mtls/tls.key
            - name: MTLS_CA_PATH
              value: /etc/mtls/ca.crt
          volumeMounts:
            - name: mtls-certs
              mountPath: /etc/mtls
              readOnly: true
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
      volumes:
        - name: mtls-certs
          secret:
            secretName: cloudcommander-worker-mtls
            optional: true
